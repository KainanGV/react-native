import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  label: {
    fontSize: 14,
    color: "#841245",
    marginBottom: 8,
  },
  input: {
    width: "100%",
    height: 44,
    paddingHorizontal: 16,
    borderWidth: 1,
    borderColor: "#A0A0A0",
    borderRadius: 8,
  },
  errorText: {
    color: "red",
    marginTop: 4,
  },
});

export { styles };
